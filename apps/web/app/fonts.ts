import { Kalam, Ranchers } from "next/font/google";

export const kalam = Kalam({
    subsets: ["latin"],
    weight: ["400", "700"],
});

export const ranchers = Ranchers({
    subsets: ["latin"],
    weight: ["400"],
});
