// @repo/ui is currently a CSS-only package (see ./styles.css, consumed by apps/web).
// This placeholder keeps the package a valid TypeScript project so `tsc --noEmit`
// has an input to check. Add shared React components here when the design system grows.
export {};
