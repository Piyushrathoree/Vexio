# Database — app uses DIRECT_URL (stable). DATABASE_URL kept for reference / pooler later.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Better Auth (http-server)
BETTER_AUTH_SECRET=""   # min 32 chars — run: openssl rand -base64 32
BETTER_AUTH_URL="http://localhost:8000"
WEB_URL="http://localhost:3001"

# Frontend (web app)
NEXT_PUBLIC_AUTH_URL="http://localhost:8000"
NEXT_PUBLIC_WS_URL="ws://localhost:8080"

# WebSocket server
WS_PORT="8080"

# OAuth — create apps at Google Cloud Console & GitHub Developer Settings
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Server
PORT="8000"
WS_PORT="8080"

# Resend (password reset + email verification)
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Vexio <onboarding@resend.dev>"
